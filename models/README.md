# models/

Place the trained model checkpoint here:

- **`best_model_focal.pt`** — DistilBERT + Focal Loss weights from the best validation epoch (~256MB)

This file is not included in the repository because it exceeds GitHub’s 100MB file size limit.

## Pretrained Model Download

The trained model checkpoint has been uploaded to Google Drive due to its large file size.

Download it here:

[Google Drive Link](https://drive.google.com/file/d/1HBJn79dWhXUu_2UM8xetkb5tQ887GhT3/view?usp=drive_link)

After downloading, place the file inside the `models/` directory:

```bash
models/
└── best_model_focal.pt